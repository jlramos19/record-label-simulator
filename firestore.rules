rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{uid}/slots/{slotId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
    match /players_preview/{uid}/client_log/{eventId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
