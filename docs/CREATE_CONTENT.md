# Create Content Canon Notes

- Create Content shows three stage columns (Sheet, Demo, Master) at once; the All Stages tab is removed.
- Each stage column displays its available-content count, stage-specific inputs, and a Start button that remains visible but disabled until requirements are met.
