# UI React (Islands)

This folder hosts the optional React "UI islands" build.

## Run

```bash
cd ui-react
npm install
npm run dev
```

## Build

```bash
cd ui-react
npm install
npm run build
```

Build output is written to `../assets/js/ui-react/` as `ui-react.js` and `ui-react.css`.
