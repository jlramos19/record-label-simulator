{
  "scripts": {
    "build": "node scripts/build-creator-portrait-manifest.mjs && tsc -p tsconfig.json",
    "optimize:portraits": "node scripts/optimize-creator-portraits.mjs",
    "report:lines": "node scripts/report-line-counts.mjs",
    "watch": "tsc -p tsconfig.json --watch",
    "start": "powershell -Command \"python -m http.server 5173\"",
    "dev:logs": "node scripts/dev-with-logs.mjs",
    "lint": "eslint . --ext .js,.mjs,.cjs"
  },
  "dependencies": {
    "puppeteer": "^24.34.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "sharp": "^0.33.5",
    "typescript": "^5.9.3"
  }
}
